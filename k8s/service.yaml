apiVersion: v1
kind: Service

metadata:
  name: berrystreet-service

spec:
  # type: NodePort
  type: LoadBalancer

  selector:
    app: berrystreet

  ports:
  # service port
  - port: 8000 
    # container port
    targetPort: 8000
    # external port
    nodePort: 30010